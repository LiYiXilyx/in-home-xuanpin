import test from 'node:test';import assert from 'node:assert/strict';import {calculateVisualMarketMetrics} from '../../src/modules/sourcing/visual-market-metrics.mjs';
test('uses only reliable non-conflict visual prices and separates anchor from other minimum',()=>{const result=calculateVisualMarketMetrics({anchor:{goods_id:'a',price_eur:10,title:'1pc cover'},matches:[{goods_id:'b',price_eur:8,title:'1pc cover',metadata_consistency:'CONSISTENT'},{goods_id:'c',price_eur:1,title:'cover',metadata_consistency:'CONFLICT'}]});assert.equal(result.anchor_listed_price_eur,10);assert.equal(result.min_other_listed_price_eur,8);assert.equal(result.min_reliable_unit_price_eur,8);assert.equal(result.reliable_unit_price_count,2);assert.equal(result.median_reliable_unit_price_eur,9);});
