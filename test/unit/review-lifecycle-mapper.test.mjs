import test from 'node:test';import assert from 'node:assert/strict';import {resolveRunExpectedCounts,mapReviewLifecycle} from '../../src/modules/sourcing/review-lifecycle-mapper.mjs';
test('accepts a final 17x5 run from frozen run metadata',()=>{assert.deepEqual(resolveRunExpectedCounts({source_file_count:17,candidate_count:85},{actualGoods:17,actualCandidates:85}),{expected_goods_count:17,expected_candidate_count:85,expected_candidates_per_goods:5,source:'RUN_METADATA'});});
test('maps current, other, and unresolved plan matches without cross-run switching',()=>{const rows=mapReviewLifecycle([{goods_id:'a'},{goods_id:'b'},{goods_id:'c'}],{currentRunId:'r1',runs:[{run_id:'r1',goods_ids:['a']},{run_id:'r2',goods_ids:['b'],batch_number:2}]});assert.equal(rows[0].navigation_action,'SWITCH_CURRENT_RUN');assert.equal(rows[1].navigation_action,'OPEN_OTHER_RUN');assert.equal(rows[2].review_plan_status,'REVIEW_PLAN_STATUS_UNKNOWN');});
