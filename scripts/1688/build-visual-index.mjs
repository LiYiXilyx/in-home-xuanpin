import path from 'node:path';import {fileURLToPath} from 'node:url';import {loadVisualWorkbookUniverse} from '../../src/modules/sourcing/visual-workbook-universe.mjs';import {createLocalVisualEmbeddingBackend} from '../../src/modules/sourcing/local-visual-embedding.mjs';import {createVisualIndexStore} from '../../src/modules/sourcing/visual-index-store.mjs';
const projectRoot=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../..'),pairs=process.argv.slice(2),args={};for(let i=0;i<pairs.length;i+=2)args[pairs[i]?.replace(/^--/,'')]=pairs[i+1];if(!args.workbook||!args['cache-root'])throw new Error('usage: --workbook <xlsx> --cache-root <dir>');
const universe=await loadVisualWorkbookUniverse({workbookPath:args.workbook}),backend=createLocalVisualEmbeddingBackend({cacheRoot:args['cache-root'],sourcePath:path.join(projectRoot,'tools/yingdao-vision-embed.swift')}),store=createVisualIndexStore({cacheRoot:args['cache-root'],embeddingBackend:backend});const result=await store.build({universe,onProgress:value=>{if(value.completed%100===0)console.error(JSON.stringify(value));}});console.log(JSON.stringify(result,null,2));
