import assert from 'node:assert/strict';import {captureProgress,MAX_SCREENS} from './capture-budget.mjs';assert.equal(MAX_SCREENS,8);const ids=Array.from({length:40},(_,i)=>String(i));assert.equal(captureProgress(ids.slice(0,10),2).reason,null);assert.equal(captureProgress([...ids.slice(0,20),...ids.slice(0,20)],7).unique,20);assert.equal(captureProgress(ids,7).reason,'TARGET_REACHED');assert.equal(captureProgress(ids.slice(0,39),8).reason,'SCREEN_LIMIT');assert.equal(captureProgress([...ids,'extra'],8).reason,'TARGET_REACHED');console.log('capture budget tests passed');