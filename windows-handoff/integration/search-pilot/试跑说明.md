# 单关键词自动试跑

用户已授权锚点商品 601099512771916 与关键词 scooter with petrol inline fuel gas filter。

正式复核页新增链接，指向 http://127.0.0.1:37822/ 。本轮固定使用上述锚点，不随正式复核页的商品切换。独立 pilot.db 及截图在 local-test/search-pilot，不修改正式 Evidence、Assessment 或冻结缓存。

在 Temu-DE-Test 中暂时停用正式“Temu Catalog 与评论采集”扩展，以免旧任务面板或自动安全信号影响试跑。加载本目录 extension。手工点“开始一次自动试跑”后，会自动打开一个搜索标签页，不滚动翻页；最多等待约35秒；读取首屏最多12件并截取商品安全区域，完成或失败后打开本机结果页。保持搜索页在前台；遇验证码、搜索词改变、无结果、超时或安全区域无法确认时停止，不自动重试。

结果页面支持逐件移除和恢复；原始截图及字段不变，操作写入独立 audit 表。没有单品图片下载、正式商品池写入和自动同类判定。浏览器加载搜索页本身仍会消耗代理流量。

本地合成截图/字段服务测试14项通过，真实 VMLogin 搜索及截图尚待用户加载扩展并点击运行，不能宣称已通过真实站点验收。

回退：停止 process.json 记录且命令行匹配本服务的进程，停用试跑扩展；将 sourcing-review.html.before 恢复至 runtime/ui/sourcing-review.html 可移除入口。保留 pilot 数据以供审计。
