'use strict';

(() => {
  function renderMarkup(model){
    const health=model.health.rows.map(row=>`<div class="health-row"><span>${escape(row.label)}</span><span>${escape(row.actual)}</span></div>`).join('');
    return `<h1>Temu 手工采集</h1><p class="category">${escape(model.categoryLabel)}</p><div class="summary"><div><span>任务状态</span><strong>${escape(model.task.status)}</strong></div><div><span>采集模式</span><strong>${escape(model.quantity.label)}</strong></div><div><span>当前已采集</span><strong>${model.quantity.currentUnique}</strong></div><div><span>绑定状态</span><strong data-binding-status="${escape(model.binding.status)}">${model.binding.status==='BOUND'?'已绑定':'未绑定'}</strong></div></div><section class="summary" aria-label="最近一次提交结果"><div><span>新增</span><strong>${model.counts.added}</strong></div><div><span>重复</span><strong>${model.counts.duplicates}</strong></div><div><span>失败</span><strong>${model.counts.failed}</strong></div></section><p>以上为最近一次提交计数，首次提交前显示0；重复商品不会重复新增。</p><section class="health" data-health-status="${escape(model.health.status)}"><h2>页面检查 · ${escape(model.health.statusLabel??model.health.status)}</h2>${health}</section>${action('detect-page','① 检测当前页面',model.steps.detect)}${action('bind-page','② 绑定当前页面',model.steps.bind)}${action('capture-page','③ 采集当前页面',model.steps.capture)}<p id="catalog-status" role="status" aria-live="polite">${escape(model.error?.reason??model.task.status)}${model.error?.action?' '+escape(model.error.action):''}</p><details id="catalog-popup-technical"><summary>技术详情</summary><dl><dt>Campaign ID</dt><dd>${escape(model.technical.campaignId??'—')}</dd><dt>Category Key</dt><dd>${escape(model.technical.categoryKey??'—')}</dd><dt>Profile</dt><dd>${escape(model.technical.profileVersion??'—')}</dd><dt>新增 / 重复 / 失败</dt><dd>${model.counts.added} · ${model.counts.duplicates} · ${model.counts.failed}</dd></dl></details><details class="legacy"><summary>旧版 / 高级浏览器连接</summary><p>Manual Bind 手工采集不需要连接 CDP。</p></details>`;
  }
  function action(id,label,step){return `<button id="${id}" type="button"${step.enabled?'':` disabled aria-disabled="true"`}>${label}</button>${step.disabledReason?`<p class="reason">${escape(step.disabledReason)}</p>`:''}`;}
  function escape(value){return String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));}
  globalThis.TemuCatalogPopupView=Object.freeze({renderMarkup});
})();
